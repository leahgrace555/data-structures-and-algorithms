const { Vertex, Edge, Graph } = require('./graph.js');



function getEdge(graph,arr) {

  // needs to check connections between cities in array
  // get the value of edge and sum for trip cost

  cityOne = 0;
  cityTwo = 1;

  for(let i = 0; i< arr.length-1; i++) {
    //
  }
}